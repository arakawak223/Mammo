# ── MamoriTalk 環境変数テンプレート ──
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env

# ── PostgreSQL ──
POSTGRES_USER=mamori
POSTGRES_PASSWORD=mamori_dev
POSTGRES_DB=mamori_talk

# ── API サービス ──
DATABASE_URL=postgresql://mamori:mamori_dev@postgres:5432/mamori_talk
REDIS_URL=redis://redis:6379
JWT_SECRET=dev-jwt-secret-change-in-production
AI_SERVICE_URL=http://ai:8000
NODE_ENV=development

# ── CORS（本番のみ、カンマ区切り） ──
# CORS_ORIGINS=https://app.mamoritalk.jp,https://admin.mamoritalk.jp

# ── AI サービス ──
ENVIRONMENT=development

# ── Firebase（プッシュ通知） ──
# FIREBASE_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
